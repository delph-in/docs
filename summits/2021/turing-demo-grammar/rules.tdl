;;; -*- Mode: TDL; Coding: utf-8 -*-
head-comp := head-comp-phrase.

subj-head := subj-head-phrase.

head-spec := head-spec-phrase.

bare-np := bare-np-phrase.

nondeterministic-pop-select := nondeterministic-pop-select-phrase.

nondeterministic-pop-increment := nondeterministic-pop-increment-phrase.